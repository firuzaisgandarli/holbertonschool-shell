#!/usr/bin/env bash
# This script displays the PID and process name of processes containing the word "bash"

for pid in /proc/[0-9]*; do
    name=$(basename "$(cat "$pid/comm" 2>/dev/null)")
    if echo "$name" | grep -q "bash"; then
        echo "$(basename "$pid") $name"
    fi
done
